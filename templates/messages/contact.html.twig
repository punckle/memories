{% block body %}

    <div class="card" style="width:400px">
        <div class="card-body">
            <h4 class="card-title text-center">{{ subject }} - {{ contact }}</h4>
            <p class="card-text text-center">{{ description }}</p>
        </div>
    </div>

{% endblock %}