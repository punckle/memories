{% extends 'base.html.twig' %}

{% block title %}Se connecter{% endblock %}

{% block body %}

    {% if error %}
        <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
    {% endif %}

    <div class="container">
        <div class="row mt-5">
            <div class="col-md-3"></div>
            <div class="col-md-6">
                <div class="bg-light py-3 px-3">
                    <h1 class="text-center my-4">Se connecter à ...</h1>


                    <form action="{{ path('login') }}" method="POST">
                        <div class="form-group">
                            <input type="text" name="_username" id="email" class="form-control" placeholder="Email" required value="{{ last_username }}">
                        </div>
                        <div class="form-group">
                            <input type="password" name="_password" id="password" class="form-control" placeholder="Mot de passe" required>
                        </div>
                        <div class="form-group">
                            <button type="submit" class="btn btn-success">Se connecter</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>

{% endblock %}